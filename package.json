{
  "name": "folio-ohip",
  "version": "1.2.0",
  "private": true,
  "description": "Local-first OHIP emergency billing workspace",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "start": "python3 -m http.server 8771 --bind 127.0.0.1",
    "build": "esbuild src/semantic-worker.js --bundle --format=esm --outfile=vendor/semantic-worker.js && esbuild src/semantic.js --bundle --format=esm --outfile=vendor/semantic.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "0.2.85"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
